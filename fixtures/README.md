# Fixtures

Synthetic fixtures for unit tests may live here or under `tests/fixtures/`.

Do not commit real RM2 raw data, real notebooks, generated PDFs, SSH configuration, logs, or production metadata.
