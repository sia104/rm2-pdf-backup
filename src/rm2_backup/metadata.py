"""RM2 metadata parsing boundaries.

Planned responsibilities:
- parse local copies of UUID metadata/content files;
- extract visible names, parent UUIDs, document types and deleted/trash state;
- avoid assuming that raw filesystem layout equals visible RM2 folder layout.
"""
