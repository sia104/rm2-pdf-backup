"""Renderer interface boundaries.

Planned responsibilities:
- define the behaviour expected from renderer backends;
- keep local rendering swappable;
- ensure renderer failures are reported without unsafe publication.
"""
