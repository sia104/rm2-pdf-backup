"""Persistent manifest and run-state boundaries.

Planned responsibilities:
- record documents seen during each run;
- track render status, hashes, output paths and failures;
- support change detection and safe re-render decisions.
"""
